{{!
    This file is part of Moodle - http://moodle.org/

    Moodle is free software: you can redistribute it and/or modify
    it under the terms of the GNU General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    @template local_timetracker/report_index

    Time Tracker report page.

    Example context (json):
    {
        "ajaxUrl": "https://example.com/local/timetracker/ajax/get_index_report.php",
        "downloadajaxurl": "https://example.com/local/timetracker/ajax/download_index.php",
        "currentuserid": 2,
        "loadinggif": "https://example.com/pix/i/loading.gif",
        "sesskey": "abc123",
        "search_placeholder": "Search a record",
        "search_go_label": "Go",
        "exportlabel": "Export",
        "exportcsv": "CSV",
        "exportexcel": "Excel",
        "recordsperpage": "Records per page",
        "previouslabel": "Previous",
        "nextlabel": "Next",
        "selectcourseprompt": "Select a course to load the report.",
        "nodataavailable": "No data available in table",
        "showingrecordsformat": "Showing %%FROM%% - %%TO%% of %%TOTAL%%",
        "colcount": 5,
        "courses": [
            {"id": 0, "fullname": "Select course"},
            {"id": 2, "fullname": "Sample course"}
        ],
        "tableheader": [
            {"name": "#"},
            {"name": "Name"},
            {"name": "Total time tracked"},
            {"name": "Activities tracked"},
            {"name": "Last tracked end"}
        ]
    }
}}
<div class="container-fluid local-timetracker-report" data-region="timetracker-report"
    data-nodata="{{nodataavailable}}" data-colcount="{{colcount}}"
    data-showingrecords="{{showingrecordsformat}}">
    <div class="tablefilters">
        <input type="hidden" name="ajaxUrl" id="ajaxUrl" value="{{{ajaxUrl}}}">
        <input type="hidden" name="downloadajaxurl" id="downloadajaxurl" value="{{{downloadajaxurl}}}">
        <input type="hidden" name="currentuserid" id="currentuserid" value="{{currentuserid}}">
        <input type="hidden" id="timetracker-sesskey" value="{{sesskey}}">
        <input type="hidden" id="pagenumber" name="pagenumber" value="1">
    </div>

    <div class="table-div">
        <div id="ajaxloading" class="ajaxloading">
            <img src="{{{loadinggif}}}" alt="{{#str}}loading, local_timetracker{{/str}}">
        </div>

        <div class="headerinfo">
            <div class="left">
                <div>
                    <select id="courseid" class="form-select me-2"
                        aria-label="{{#str}}select_course, local_timetracker{{/str}}">
                        {{#courses}}
                            <option value="{{id}}">{{fullname}}</option>
                        {{/courses}}
                    </select>
                </div>
                <div class="searchbox d-flex align-items-center">
                    <label for="searchdata" class="visually-hidden">{{search_placeholder}}</label>
                    <input type="search" id="searchdata" class="form-control"
                        placeholder="{{search_placeholder}}" aria-label="{{search_placeholder}}">
                    <button type="button" class="btn btn-secondary" id="btnsearch">{{search_go_label}}</button>
                </div>
            </div>
            <div class="right">
                <div class="timetracker-tables-info">
                    <div class="dropdown">
                        <button class="btn btn-secondary timetracker-export" type="button"
                            data-bs-toggle="dropdown" aria-expanded="false" id="timetracker-export">
                            {{exportlabel}}
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" id="dataexport"
                            aria-labelledby="timetracker-export">
                            <li data-export="excel"><span class="dropdown-item">{{exportexcel}}</span></li>
                            <li data-export="csv"><span class="dropdown-item">{{exportcsv}}</span></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div id="timetracker-empty" class="alert alert-info mb-3">{{selectcourseprompt}}</div>

        <div class="report_table_wrapper">
            <table id="timetracker-index-table" class="table generaltable table-striped table-hover w-100">
                <thead>
                    <tr>
                        {{#tableheader}}
                            <th>{{name}}</th>
                        {{/tableheader}}
                    </tr>
                </thead>
                <tbody></tbody>
            </table>
        </div>

        <div class="footerinfo">
            <div class="left">{{recordsperpage}}
                <select name="rec_per_page" id="rec_per_page" class="form-select d-inline-block w-auto">
                    <option value="10">10</option>
                    <option value="25">25</option>
                    <option value="50">50</option>
                    <option value="100">100</option>
                </select>
            </div>
            <div class="right">
                <div class="timetracker-tables-info timetracker-paging-controls">
                    <span class="timetracker-paging-summary" aria-live="polite"></span>
                    <nav aria-label="{{#str}}pagedcontentnavigation, core{{/str}}">
                        <ul class="pagination mb-0">
                            <li class="page-item disabled" id="timetracker-prev-item">
                                <a href="#" class="page-link timetracker-page-previous" id="timetracker-prev"
                                    aria-label="{{#str}}previouspage, moodle{{/str}}" tabindex="-1"
                                    aria-disabled="true">
                                    <span class="icon-no-margin dir-rtl-hide" aria-hidden="true">{{#pix}} i/previous, core {{/pix}}</span>
                                    <span class="icon-no-margin dir-ltr-hide" aria-hidden="true">{{#pix}} i/next, core {{/pix}}</span>
                                    <span class="visually-hidden">{{previouslabel}}</span>
                                </a>
                            </li>
                            <li class="page-item disabled" id="timetracker-next-item">
                                <a href="#" class="page-link timetracker-page-next" id="timetracker-next"
                                    aria-label="{{#str}}nextpage, moodle{{/str}}" tabindex="-1"
                                    aria-disabled="true">
                                    <span class="icon-no-margin dir-rtl-hide" aria-hidden="true">{{#pix}} i/next, core {{/pix}}</span>
                                    <span class="icon-no-margin dir-ltr-hide" aria-hidden="true">{{#pix}} i/previous, core {{/pix}}</span>
                                    <span class="visually-hidden">{{nextlabel}}</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>
